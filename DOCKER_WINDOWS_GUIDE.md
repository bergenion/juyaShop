# Подробная инструкция: Запуск приложения через Docker на Windows

Это пошаговая инструкция по запуску бэкенда и фронтенда приложения JuyaShop через Docker на Windows.

---

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка](#подготовка)
3. [Настройка базы данных](#настройка-базы-данных)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Сборка и запуск через Docker](#сборка-и-запуск-через-docker)
6. [Проверка работы](#проверка-работы)
7. [Управление контейнерами](#управление-контейнерами)
8. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

### Необходимое программное обеспечение

1. **Windows 10/11** (64-bit)
2. **Docker Desktop для Windows**
3. **Git для Windows** (опционально, если клонируете репозиторий)
4. **PostgreSQL база данных** (уже установлена на вашем сервере)

### Минимальные системные требования

- **RAM**: минимум 4 ГБ (рекомендуется 8 ГБ)
- **Диск**: минимум 10 ГБ свободного места
- **Процессор**: 64-bit с поддержкой виртуализации

---

## 📦 Подготовка

### Шаг 1: Установка Docker Desktop

**Среда выполнения: Windows (PowerShell от администратора)**

#### Вариант 1: Через официальный сайт

1. Перейдите на https://www.docker.com/products/docker-desktop/
2. Скачайте **Docker Desktop для Windows**
3. Запустите установщик `Docker Desktop Installer.exe`
4. Следуйте инструкциям установщика
5. **Перезагрузите компьютер** после установки

#### Вариант 2: Через winget (Windows 10/11)

```powershell
# Откройте PowerShell от администратора
winget install --id Docker.DockerDesktop -e --source winget
```

#### Проверка установки

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Проверьте версию Docker
docker --version

# Проверьте версию Docker Compose
docker-compose --version

# Проверьте, что Docker запущен
docker ps
```

**Ожидаемый результат:**
```
Docker version 24.0.0 or higher
Docker Compose version v2.20.0 or higher
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
```

Если команды не работают:
1. Убедитесь, что Docker Desktop запущен (иконка в системном трее)
2. Дождитесь полного запуска Docker Desktop (иконка перестанет мигать)
3. Перезапустите PowerShell/Command Prompt

---

### Шаг 2: Получение кода приложения

**Среда выполнения: Windows (PowerShell или Command Prompt)**

#### Вариант 1: Клонирование из Git

```powershell
# Перейдите в нужную директорию (например, C:\Projects)
cd C:\Projects

# Клонируйте репозиторий
git clone https://github.com/your-username/juyaShop.git

# Перейдите в директорию проекта
cd juyaShop
```

#### Вариант 2: Распаковка архива

1. Скачайте архив проекта
2. Распакуйте в нужную директорию (например, `E:\services\juyashop\juyaShop`)
3. Откройте PowerShell в этой директории

```powershell
# Перейдите в директорию проекта
cd E:\services\juyashop\juyaShop
```

#### Проверка структуры проекта

**Среда выполнения: Windows (PowerShell)**

```powershell
# Проверьте, что все необходимые файлы на месте
Get-ChildItem -Name

# Должны быть видны:
# - backend/
# - frontend/
# - docker-compose.yml
# - env.example
```

---

## 🗄️ Настройка базы данных

### Шаг 1: Подготовка PostgreSQL

**Среда выполнения: Ваш PostgreSQL сервер**

Убедитесь, что у вас есть:
1. **Хост базы данных** (например, `localhost` или IP адрес)
2. **Порт** (обычно `5432`)
3. **Имя базы данных** (например, `juya_shop`)
4. **Имя пользователя** (например, `postgres`)
5. **Пароль** пользователя

### Шаг 2: Создание базы данных (если нужно)

**Среда выполнения: PostgreSQL (psql или pgAdmin)**

```sql
-- Подключитесь к PostgreSQL
-- Создайте базу данных
CREATE DATABASE juya_shop;

-- Создайте пользователя (если нужно)
CREATE USER juya_user WITH PASSWORD 'your_password';

-- Дайте права пользователю
GRANT ALL PRIVILEGES ON DATABASE juya_shop TO juya_user;
```

**Или через командную строку:**

```powershell
# На Windows, если PostgreSQL установлен локально
psql -U postgres

# Затем выполните SQL команды выше
```

---

## ⚙️ Настройка переменных окружения

### Шаг 1: Создание .env файла

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Убедитесь, что вы в корневой директории проекта
cd E:\services\juyashop\juyaShop

# Скопируйте пример файла
Copy-Item env.example .env

# Откройте файл для редактирования
notepad .env
```

### Шаг 2: Заполнение переменных окружения

**Среда выполнения: Блокнот (notepad) или любой текстовый редактор**

Откройте файл `.env` и заполните следующие переменные:

```env
# Database
# Формат: postgresql://username:password@host:port/database_name?schema=public
# Пример для локальной базы:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/juya_shop?schema=public

# Пример для удаленной базы:
# DATABASE_URL=postgresql://username:password@192.168.1.100:5432/juya_shop?schema=public

# JWT Secret (используйте длинную случайную строку)
# Сгенерируйте секрет командой ниже или используйте любой длинный случайный текст
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-change-this

# Backend Port (обычно не нужно менять)
PORT=3000

# Frontend URL (для CORS)
# Для локальной разработки:
FRONTEND_URL=http://localhost

# Для продакшна (замените на ваш домен):
# FRONTEND_URL=http://your-domain.com
```

### Шаг 3: Генерация JWT Secret (опционально)

**Среда выполнения: Windows (PowerShell)**

```powershell
# Генерация случайного секрета (32 символа)
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})

# Или более длинный (64 символа)
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | ForEach-Object {[char]$_})
```

Скопируйте сгенерированную строку и вставьте в `.env` файл вместо `your-super-secret-jwt-key-minimum-32-characters-long-change-this`.

### Шаг 4: Проверка .env файла

**Среда выполнения: Windows (PowerShell)**

```powershell
# Проверьте содержимое файла (пароль не будет показан полностью)
Get-Content .env

# Убедитесь, что файл содержит все необходимые переменные:
# - DATABASE_URL
# - JWT_SECRET
# - PORT
# - FRONTEND_URL
```

**Важно:**
- Файл `.env` должен находиться в **корневой директории проекта** (там, где `docker-compose.yml`)
- Не коммитьте `.env` файл в Git (он уже в `.gitignore`)
- Используйте сильные пароли и секреты в продакшне

---

## 🐳 Сборка и запуск через Docker

### Шаг 1: Проверка Docker

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Убедитесь, что Docker Desktop запущен
docker ps

# Если команда работает, Docker готов к использованию
# Если ошибка, запустите Docker Desktop и подождите его полной загрузки
```

### Шаг 2: Сборка Docker образов

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Убедитесь, что вы в корневой директории проекта
cd E:\services\juyashop\juyaShop

# Соберите образы для backend и frontend
# Это займет 5-10 минут при первой сборке
docker-compose build
```

**Что происходит:**
1. Загружаются базовые образы (node:20-alpine, nginx:alpine)
2. Устанавливаются зависимости для backend
3. Устанавливаются зависимости для frontend
4. Собирается backend приложение
5. Собирается frontend приложение

**Ожидаемый вывод:**
```
[+] Building 293.8s (32/32) FINISHED
 => [backend builder 1/8] FROM docker.io/library/node:20-alpine
 => [frontend builder 1/6] FROM docker.io/library/node:20-alpine
 ...
 => => naming to docker.io/library/juya-shop-backend
 => => naming to docker.io/library/juya-shop-frontend
```

**Если сборка занимает много времени:**
- Это нормально для первой сборки (5-10 минут)
- Последующие сборки будут быстрее благодаря кэшированию
- Убедитесь, что у вас стабильное интернет-соединение

### Шаг 3: Запуск контейнеров

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Запустите контейнеры в фоновом режиме
docker-compose up -d
```

**Что происходит:**
1. Создаются контейнеры из собранных образов
2. Запускается backend контейнер на порту 3000
3. Запускается frontend контейнер на порту 80
4. Выполняются миграции базы данных (автоматически)
5. Приложение готово к использованию

**Ожидаемый вывод:**
```
[+] Running 3/3
 ✔ Network juya-shop_juya-shop-network    Created
 ✔ Container juya-shop-backend             Started
 ✔ Container juya-shop-frontend            Started
```

### Шаг 4: Проверка статуса контейнеров

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Проверьте статус всех контейнеров
docker ps

# Ожидаемый результат:
# CONTAINER ID   IMAGE                    STATUS         PORTS                    NAMES
# xxxxxxxxxxxx   juya-shop-frontend       Up 2 minutes   0.0.0.0:80->80/tcp      juya-shop-frontend
# xxxxxxxxxxxx   juya-shop-backend        Up 2 minutes   0.0.0.0:3000->3000/tcp   juya-shop-backend
```

**Если контейнеры не запущены:**
```powershell
# Проверьте логи для выявления проблемы
docker-compose logs
```

---

## ✅ Проверка работы

### Шаг 1: Проверка Backend

**Среда выполнения: Браузер или PowerShell**

#### Вариант 1: Через браузер

Откройте в браузере:
```
http://localhost:3000/api/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-07T12:00:00.000Z"
}
```

#### Вариант 2: Через PowerShell

```powershell
# Проверьте health endpoint
Invoke-WebRequest -Uri "http://localhost:3000/api/health" -UseBasicParsing

# Ожидаемый результат:
# StatusCode        : 200
# StatusDescription : OK
```

### Шаг 2: Проверка Frontend

**Среда выполнения: Браузер**

Откройте в браузере:
```
http://localhost
```

**Ожидаемый результат:**
- Открывается главная страница приложения
- Интерфейс загружается корректно
- Нет ошибок в консоли браузера (F12 → Console)

### Шаг 3: Проверка логов

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Просмотр логов всех контейнеров
docker-compose logs

# Просмотр логов только backend
docker-compose logs backend

# Просмотр логов только frontend
docker-compose logs frontend

# Просмотр логов в реальном времени
docker-compose logs -f
```

**Ожидаемые логи backend:**
```
[Nest] INFO  [NestFactory] Starting Nest application...
[Nest] INFO  [InstanceLoader] AppModule dependencies initialized
[Nest] INFO  [NestFactory] Nest application successfully started
```

**Ожидаемые логи frontend:**
```
/docker-entrypoint.sh: Configuration complete; ready for start up
```

---

## 🎮 Управление контейнерами

### Остановка контейнеров

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Остановить контейнеры (приложение перестанет работать)
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить, удалить контейнеры и volumes (ОСТОРОЖНО: удалит данные!)
docker-compose down -v
```

### Перезапуск контейнеров

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Перезапустить контейнеры
docker-compose restart

# Или остановить и запустить заново
docker-compose down
docker-compose up -d
```

### Обновление приложения

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# 1. Остановите контейнеры
docker-compose down

# 2. Обновите код (если используете Git)
git pull origin main

# 3. Пересоберите образы
docker-compose build

# 4. Запустите контейнеры
docker-compose up -d
```

### Просмотр использования ресурсов

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Просмотр использования CPU, памяти и сети
docker stats

# Остановить просмотр: Ctrl+C
```

### Вход в контейнер

**Среда выполнения: Windows (PowerShell или Command Prompt)**

```powershell
# Войти в backend контейнер
docker exec -it juya-shop-backend sh

# Войти в frontend контейнер
docker exec -it juya-shop-frontend sh

# Выполнить команду в контейнере без входа
docker exec juya-shop-backend npx prisma studio
```

---

## 🔧 Решение проблем

### Проблема 1: Docker Desktop не запускается

**Симптомы:**
- Docker команды не работают
- Ошибка "Cannot connect to the Docker daemon"

**Решение:**

1. **Проверьте, что Docker Desktop запущен:**
   - Найдите иконку Docker в системном трее
   - Если иконки нет, запустите Docker Desktop из меню Пуск

2. **Проверьте виртуализацию:**
   - Убедитесь, что виртуализация включена в BIOS
   - Проверьте в диспетчере задач → Производительность → CPU → Виртуализация: Включено

3. **Перезапустите Docker Desktop:**
   - Правый клик на иконке Docker → Quit Docker Desktop
   - Запустите Docker Desktop снова
   - Дождитесь полной загрузки (иконка перестанет мигать)

### Проблема 2: Порты заняты

**Симптомы:**
- Ошибка "port is already allocated"
- Контейнеры не запускаются

**Решение:**

```powershell
# Проверьте, какие процессы используют порты
netstat -ano | findstr ":3000"
netstat -ano | findstr ":80"

# Если порты заняты, либо:
# 1. Остановите процесс, использующий порт
# 2. Или измените порты в docker-compose.yml
```

**Изменение портов в docker-compose.yml:**

```yaml
services:
  backend:
    ports:
      - "3001:3000"  # Измените 3000 на 3001 (или другой свободный порт)
  
  frontend:
    ports:
      - "8080:80"  # Измените 80 на 8080 (или другой свободный порт)
```

### Проблема 3: Ошибка подключения к базе данных

**Симптомы:**
- Backend не запускается
- Ошибки в логах: "Can't reach database server"

**Решение:**

1. **Проверьте DATABASE_URL в .env файле:**
   ```powershell
   Get-Content .env
   ```

2. **Проверьте подключение к базе данных:**
   ```powershell
   # Если PostgreSQL на том же компьютере
   psql -U postgres -h localhost
   
   # Если PostgreSQL на удаленном сервере
   # Убедитесь, что сервер доступен и порт 5432 открыт
   ```

3. **Проверьте логи backend:**
   ```powershell
   docker-compose logs backend
   ```

4. **Убедитесь, что база данных создана:**
   ```sql
   -- Подключитесь к PostgreSQL
   \l  -- Список баз данных
   ```

### Проблема 4: Контейнеры постоянно перезапускаются

**Симптомы:**
- Статус контейнера: "Restarting"
- Приложение не работает

**Решение:**

```powershell
# Проверьте логи для выявления ошибки
docker-compose logs backend
docker-compose logs frontend

# Проверьте статус контейнеров
docker ps -a

# Остановите контейнеры
docker-compose down

# Проверьте .env файл
Get-Content .env

# Пересоберите и запустите заново
docker-compose build --no-cache
docker-compose up -d
```

### Проблема 5: Frontend не открывается

**Симптомы:**
- Страница не загружается
- Ошибка "This site can't be reached"

**Решение:**

1. **Проверьте, что frontend контейнер запущен:**
   ```powershell
   docker ps
   ```

2. **Проверьте логи frontend:**
   ```powershell
   docker-compose logs frontend
   ```

3. **Проверьте, что backend доступен:**
   ```powershell
   Invoke-WebRequest -Uri "http://localhost:3000/api/health" -UseBasicParsing
   ```

4. **Попробуйте открыть через IP адрес:**
   ```
   http://127.0.0.1
   ```

### Проблема 6: Миграции не выполняются

**Симптомы:**
- Ошибки в логах backend о миграциях
- Таблицы не созданы в базе данных

**Решение:**

```powershell
# Выполните миграции вручную
docker exec juya-shop-backend npx prisma migrate deploy

# Или через docker-compose
docker-compose exec backend npx prisma migrate deploy

# Проверьте статус миграций
docker exec juya-shop-backend npx prisma migrate status
```

### Проблема 7: Медленная сборка

**Симптомы:**
- Сборка занимает очень много времени (>15 минут)

**Решение:**

1. **Проверьте интернет-соединение:**
   - Медленный интернет замедляет загрузку образов

2. **Используйте кэш (не используйте --no-cache без необходимости):**
   ```powershell
   # Быстрая сборка (с кэшем)
   docker-compose build
   
   # Полная пересборка (без кэша, медленнее)
   docker-compose build --no-cache
   ```

3. **Очистите неиспользуемые образы:**
   ```powershell
   docker image prune -a -f
   ```

---

## 📊 Полезные команды

### Мониторинг

```powershell
# Просмотр логов в реальном времени
docker-compose logs -f

# Просмотр использования ресурсов
docker stats

# Просмотр информации о контейнерах
docker ps -a

# Просмотр информации об образах
docker images
```

### Очистка

```powershell
# Удалить остановленные контейнеры
docker container prune -f

# Удалить неиспользуемые образы
docker image prune -a -f

# Удалить неиспользуемые volumes
docker volume prune -f

# Полная очистка (ОСТОРОЖНО!)
docker system prune -a --volumes -f
```

### Отладка

```powershell
# Просмотр логов конкретного контейнера
docker logs juya-shop-backend
docker logs juya-shop-frontend

# Просмотр последних 100 строк логов
docker logs --tail 100 juya-shop-backend

# Выполнение команды в контейнере
docker exec juya-shop-backend npx prisma studio
docker exec juya-shop-backend sh
```

---

## 🎯 Быстрый старт (краткая версия)

Если вы уже знакомы с Docker, вот краткая версия:

```powershell
# 1. Перейдите в директорию проекта
cd E:\services\juyashop\juyaShop

# 2. Создайте .env файл
Copy-Item env.example .env
notepad .env  # Заполните переменные

# 3. Соберите и запустите
docker-compose up -d --build

# 4. Проверьте
Start-Process "http://localhost"
```

---

## 📚 Дополнительная информация

- **Подробная инструкция по деплою**: `DEPLOY_PRODUCTION.md`
- **Автоматический деплой**: `AUTO_DEPLOY_SETUP.md`
- **Решение проблем**: `TROUBLESHOOTING_DEPLOY.md`

---

## ❓ Вопросы и поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что все требования выполнены
4. Проверьте, что `.env` файл заполнен правильно

---

**Успешного запуска! 🚀**


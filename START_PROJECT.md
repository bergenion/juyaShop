# 🚀 Пошаговая инструкция запуска проекта

## Шаг 1: Настройка переменных окружения

### Backend (.env файл в папке `backend/`)

Убедитесь, что файл `backend/.env` содержит:

```env
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3000
NODE_ENV=development
FRONTEND_URL="http://localhost:5173"
```

**Важно:** Замените в `DATABASE_URL`:
- `username` - ваш пользователь PostgreSQL
- `password` - ваш пароль PostgreSQL  
- `database_name` - имя вашей базы данных

### Frontend (.env файл в папке `frontend/`)

Убедитесь, что файл `frontend/.env` содержит:

```env
VITE_API_URL=http://localhost:3000/api
```

---

## Шаг 2: Проверка подключения к базе данных

```bash
cd backend
npm run test:db
```

Если подключение успешно, вы увидите информацию о версии PostgreSQL, базе данных и таблицах.

---

## Шаг 3: Генерация Prisma Client

```bash
cd backend
npx prisma generate
```

---

## Шаг 4: Запуск миграций

```bash
cd backend
npx prisma migrate dev
```

Эта команда создаст все таблицы в базе данных.

---

## Шаг 5: Заполнение базы тестовыми данными

```bash
cd backend
npm run prisma:seed
```

Создаст:
- Администратора: `admin@example.com` / `admin123`
- Пользователя: `user@example.com` / `user123`
- 10 тестовых товаров

---

## Шаг 6: Запуск Backend

В первом терминале:

```bash
cd backend
npm run start:dev
```

Вы должны увидеть:
```
✅ Подключение к базе данных установлено успешно
🚀 Backend запущен на http://localhost:3000
```

---

## Шаг 7: Запуск Frontend

Во втором терминале:

```bash
cd frontend
npm run dev
```

Frontend запустится на `http://localhost:5173`

---

## Быстрый запуск (если всё уже настроено)

Из корневой директории проекта:

```bash
# Запуск обоих серверов одновременно
npm run dev
```

---

## Проверка работоспособности

1. **Backend Health Check:**
   - Откройте в браузере: http://localhost:3000/api/health
   - Должен вернуться JSON с `"status": "ok"`

2. **Frontend:**
   - Откройте: http://localhost:5173
   - Должна открыться главная страница магазина

3. **Тест авторизации:**
   - Перейдите на страницу входа
   - Войдите как админ: `admin@example.com` / `admin123`

---

## Возможные проблемы

### Ошибка подключения к БД
- Проверьте, что PostgreSQL сервер запущен
- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что база данных существует

### Ошибка "Prisma Client не сгенерирован"
```bash
cd backend
npx prisma generate
```

### Ошибка миграций
```bash
cd backend
npx prisma migrate reset  # ВНИМАНИЕ: удалит все данные!
npx prisma migrate dev
```

### Порт уже занят
- Backend: измените `PORT` в `backend/.env`
- Frontend: измените порт в `frontend/vite.config.ts`

---

## Полезные команды

```bash
# Prisma Studio (визуальный редактор БД)
cd backend
npm run prisma:studio

# Проверка подключения к БД
cd backend
npm run test:db

# Пересоздание базы данных (удалит все данные!)
cd backend
npx prisma migrate reset
```


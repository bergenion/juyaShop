# 📋 Инструкция по запуску проекта Истоки Леса

## 🔧 Требования

- Node.js 18+ и npm
- Docker и Docker Compose (для продакшн режима)
- PostgreSQL база данных на `192.168.3.124:5432`

---

## 🚀 Режим разработки (Development)

### Шаг 1: Установка зависимостей

Установите зависимости для всех частей проекта:

```bash
npm run install:all
```

Или установите вручную:

```bash
# Установка корневых зависимостей
npm install

# Установка зависимостей backend
cd backend
npm install

# Установка зависимостей frontend
cd ../frontend
npm install
```

### Шаг 2: Настройка переменных окружения

#### Backend

Создайте файл `.env` в папке `backend/`:

```bash
cd backend
cp ../env.example .env
```

Отредактируйте `backend/.env`:

```env
# Database
DATABASE_URL="postgresql://juyauser:123qwe@192.168.3.124:5432/juyashop"

# JWT
JWT_SECRET="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwiZW1haWwiOiJ1c2VyQGV4YW1wbGUuY29tIiwicm9sZSI6InVzZXIiLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MTcwMDAwMzYwMH0.3l1pQ0y7pZ2p7pZC3u3pQk8xZlZlYk9vZk8xYk9vZk8xYk9vZk8xYk9vZk8"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
NODE_ENV=development

# Frontend
FRONTEND_URL="http://localhost:5173"

```

**Важно:** Замените в `DATABASE_URL`:
- `username` - ваш пользователь PostgreSQL
- `password` - ваш пароль PostgreSQL
- `database_name` - имя вашей базы данных

#### Frontend

Создайте файл `.env` в папке `frontend/` (опционально, так как API URL уже настроен в коде):

```bash
cd frontend
```

Файл `.env` не обязателен, так как в режиме разработки API автоматически проксируется на `localhost:3000` через Vite.

### Шаг 3: Подготовка базы данных

#### Проверка подключения к базе данных

```bash
cd backend
npm run test:db
```

Если подключение успешно, вы увидите информацию о версии PostgreSQL и таблицах.

#### Генерация Prisma Client

```bash
cd backend
npm run prisma:generate
```

#### Запуск миграций

```bash
cd backend
npm run prisma:migrate
```

Эта команда создаст все таблицы в базе данных.

#### Заполнение тестовыми данными (опционально)

```bash
cd backend
npm run prisma:seed
```

Создаст:
- Администратора: `admin@example.com` / `admin123`
- Пользователя: `user@example.com` / `user123`
- 10 тестовых товаров

### Шаг 4: Запуск приложения

#### Вариант 1: Запуск через корневой package.json (оба сервера одновременно)

```bash
# Из корневой директории проекта
npm run dev
```

Эта команда запустит одновременно:
- Backend на `http://localhost:3000`
- Frontend на `http://localhost:5173`

#### Вариант 2: Запуск отдельно

**Терминал 1 - Backend:**

```bash
cd backend
npm run start:dev
```

Вы должны увидеть:
```
🚀 Backend запущен на http://localhost:3000
✅ Подключение к базе данных установлено успешно
```

**Терминал 2 - Frontend:**

```bash
cd frontend
npm run dev
```

Вы должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### Шаг 5: Проверка работы

Откройте в браузере:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3000/api

---

## 🐳 Режим продакшн (Production через Docker)

### Шаг 1: Подготовка переменных окружения

Создайте файл `.env` в корне проекта (для docker-compose):

```bash
cp env.example .env
```

Отредактируйте `.env`:

```env
DATABASE_URL=postgresql://username:password@192.168.3.124:5432/database_name?schema=public
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=3000
FRONTEND_URL=http://localhost:5173
```

**Важно:** 
- База данных всегда должна быть на `192.168.3.124:5432`
- Замените учетные данные для подключения к базе данных

### Шаг 2: Проверка подключения к базе данных

Убедитесь, что база данных доступна с вашего хоста:

```bash
# Для проверки (если у вас установлен psql)
psql -h 192.168.3.124 -p 5432 -U username -d database_name
```

### Шаг 3: Сборка Docker образов

```bash
docker-compose build
```

Эта команда соберет образы для:
- Backend (NestJS приложение)
- Frontend (React приложение с nginx)

### Шаг 4: Запуск контейнеров

```bash
docker-compose up -d
```

Флаг `-d` запускает контейнеры в фоновом режиме (detached mode).

### Шаг 5: Проверка статуса

```bash
docker-compose ps
```

Вы должны увидеть два запущенных контейнера:
- `juyashop-backend` - порт 3000
- `juyashop-frontend` - порт 5173

### Шаг 6: Просмотр логов

```bash
# Логи всех контейнеров
docker-compose logs -f

# Логи только backend
docker-compose logs -f backend

# Логи только frontend
docker-compose logs -f frontend
```

### Шаг 7: Проверка работы

Откройте в браузере:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3000/api

---

## 🔄 Управление Docker контейнерами

### Остановка контейнеров

```bash
docker-compose stop
```

### Запуск остановленных контейнеров

```bash
docker-compose start
```

### Перезапуск контейнеров

```bash
docker-compose restart
```

### Остановка и удаление контейнеров

```bash
docker-compose down
```

### Пересборка и перезапуск

```bash
docker-compose up -d --build
```

### Очистка (удаление контейнеров, сетей и образов)

```bash
docker-compose down --rmi all
```

---

## 🛠️ Полезные команды

### Backend

```bash
cd backend

# Запуск в режиме разработки
npm run start:dev

# Сборка проекта
npm run build

# Запуск в продакшн режиме
npm run start:prod

# Запуск миграций
npm run prisma:migrate

# Генерация Prisma Client
npm run prisma:generate

# Заполнение тестовыми данными
npm run prisma:seed

# Проверка подключения к БД
npm run test:db
```

### Frontend

```bash
cd frontend

# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Предпросмотр продакшн сборки
npm run preview
```

---

## 🐛 Решение проблем

### Проблема: Backend не может подключиться к базе данных

**Решение:**
1. Проверьте, что база данных доступна на `192.168.3.124:5432`
2. Убедитесь, что в `.env` файле правильный `DATABASE_URL`
3. Проверьте firewall и сетевые настройки

### Проблема: Frontend не может обратиться к API

**В режиме разработки:**
- Убедитесь, что backend запущен на `localhost:3000`
- Проверьте настройки прокси в `vite.config.ts`

**В режиме продакшн:**
- Убедитесь, что оба контейнера запущены: `docker-compose ps`
- Проверьте логи: `docker-compose logs frontend`

### Проблема: Ошибки при миграциях Prisma

**Решение:**
```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
```

Если миграции не применяются, попробуйте:
```bash
npx prisma migrate reset  # ⚠️ ВНИМАНИЕ: Удалит все данные!
```

### Проблема: Docker контейнеры не собираются

**Решение:**
1. Убедитесь, что у вас установлен Docker и Docker Compose
2. Проверьте логи сборки: `docker-compose build --no-cache`
3. Убедитесь, что все файлы на месте (Dockerfile, package.json и т.д.)

---

## 📝 Важные замечания

1. **База данных:** Всегда находится на `192.168.3.124:5432` - не меняйте этот адрес
2. **API адрес:** Frontend всегда отправляет запросы на `localhost:3000` в режиме разработки
3. **Frontend URL:** Веб-интерфейс доступен на `http://localhost:5173`
4. **CORS:** Настроен для работы с `http://localhost:5173`
5. **Загруженные файлы:** Хранятся в `backend/uploads/products/`

---

## 🎯 Быстрый старт

### Development:
```bash
npm run install:all
cd backend && npm run prisma:generate && npm run prisma:migrate && cd ..
npm run dev
```

### Production:
```bash
# Создайте .env файл с правильными данными БД
cp env.example .env
# Отредактируйте .env

docker-compose build
docker-compose up -d
```

---

**Удачной разработки! 🚀**

 #test build#